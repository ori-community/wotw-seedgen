#hidden
#name("Delta Time")
#description("Adds a tick function with delta time")

!timer(active, delta)

on reload store(active, true)

on delta > 0 {
    tick(delta)
    store(delta, 0)
}

!export(tick)
fun tick(delta: Float) {}
